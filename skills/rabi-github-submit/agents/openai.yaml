interface:
  display_name: "Rabi GitHub Submit"
  short_description: "Submit RabiRoute with one patch version per commit."
  default_prompt: "Use $rabi-github-submit to prepare a RabiRoute commit with the mandatory patch bump and bilingual version log."
