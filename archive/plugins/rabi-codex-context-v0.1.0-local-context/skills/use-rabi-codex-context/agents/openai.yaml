interface:
  display_name: "Use Rabi Codex Context"
  short_description: "Bind a Codex session to a Rabi persona context"
  default_prompt: "Use $use-rabi-codex-context to bind this Codex session to a named Rabi persona."
