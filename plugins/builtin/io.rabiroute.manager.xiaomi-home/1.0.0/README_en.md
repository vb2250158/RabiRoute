# Xiaomi Home Manager plugin

The single `xiaomiHome` business entry point for RabiRoute. It exposes Home Assistant-backed resource discovery, state reads, and typed capability actions. Credentials remain in the local runtime environment and writes are disabled by default.
