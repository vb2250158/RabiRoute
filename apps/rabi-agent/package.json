{
  "name": "rabi-agent",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Headless LAN worker that forwards RabiRoute tasks to a bound local Codex Desktop or DSH session.",
  "engines": {
    "node": ">=22.13.0"
  },
  "dependencies": { "@openai/codex": "0.144.1", "bonjour-service": "1.4.4" },
  "scripts": {
    "start": "node rabi-agent.mjs --run",
    "bootstrap": "node rabi-agent.mjs --bootstrap",
    "check": "node --check rabi-agent.mjs && node --check lib/codex-desktop-ipc.mjs && node --check lib/cwd-policy.mjs && node --test test/*.test.mjs"
  }
}
