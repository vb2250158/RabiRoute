/**
 * Compatibility facade for the canonical persona-scoped conversation store.
 * Keep storage, migration, archive, query, and formatting semantics in one module.
 */
export * from "./messageContextStore.js";
