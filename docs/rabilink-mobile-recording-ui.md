# Rabi 移动端：全天记录、消息与设备

事件列表支持双向游标分页，每次追加 100 条，跨空日期直到真实首尾；内存保留最多 1000 条，移出的页可重新加载。按时间和事件 ID 排序，同毫秒记录不会被跳过。录音独立显示，保留与视频的关联；读取失败保留列表和播放器。分页仍需枚举已有元数据，不代表已完成持久化索引。

[English](rabilink-mobile-recording-ui_en.md) | 简体中文

> 状态：0.3.30-dev 已安装；记录双页导航、顶部开关、统一回看、手机录音和声波完成本轮真机验证。眼镜音频、PC 转写端到端和全天长稳仍待验收。下文保留历史证据与当前验证范围。完整合同见[全天记录设计、迁移与验收](rabilink-all-day-recording.md)。


连续时间尺使用固定游标显示当前时刻，可横向惯性滚动跨日回看、双指缩放或双击放大；到达最新时刻进入实时，也可点击“回到实时”。绿色表示已保存录音，青色表示录像，空白表示缺口。下方事件随可见时间范围刷新；点击事件把游标与播放器定位到该记录，播放进度同步时间尺。历史录音按时间窗口加载，不再受最近 500 条限制。

预览区固定为同一高度，实时、加载、空缺、音频和视频回看切换时，日期栏、时间尺及事件列表保持原位。回看控制置于预览底部，时长置于顶部；分享收进“⋯”记录操作菜单，不再追加整行按钮。

音频和视频共用播放器浮层与唯一混合时间轴。进入或切换记录时控件默认隐藏，点击一次预览显示，再点一次隐藏；播放时 3.5 秒后自动收起，启用触摸探索时不自动隐藏。播放进度同步下方时间尺，不增加单独片段进度条。控件覆盖在内容上方，显隐不改变预览高度。

## 自动选源与统一回看（0.3.26-dev）

音频模式不再提供手动来源选择。明确开始后先使用手机；已授权眼镜提供 CXR-L 音频时切换到眼镜，断连或 5 秒未收到数据时回到手机。切换先封口旧记录，再创建具有真实物理来源的新记录；暂停后不因设备重连重新采集。眼镜连接通道仍需首次授权。

时间线页上方固定预览、日期和可拖拽时间轴，最右端为实时，向左回看历史。纯音频时显示与 WebGUI ASR 收录一致的柱状声波；实时声波读取同一采集链路的 PCM 音量，历史声波读取保存的 WAV，标出播放位置。没有声音或画面时显示“无画面”。下方按时间显示录像缩略图、录音及关联 ASR 文字；点击卡片定位上方播放器。纯录音、带声音录像共用播放器，原始视频分段可跨段定位。手机音频按已保存分片的时间与实际字节时长映射；无记录时显示空缺。

实时页随采集状态更新；仅在可见实时页每 30 秒刷新一次已验证电脑的转写，仍限最近 24 小时、最多 200 条。退出页面停止刷新。ASR 保留供应商说话人信息；当前合同没有整条录音内的句子偏移，点击文字定位所属记录，不伪造逐字对齐。视频派生音轨按父 captureId 关联，避免重复显示同一录像的派生录音。历史视频的系统时钟映射沿用会话时间，尚无逐帧墙钟同步证明。

分片 MP4 时长从实际帧时间读取，回放生成可拖动的本地缓存，保留原文件。待传音频字节统计由队列维护，避免每次写入重读全部历史分片。

源码回归、安装及本轮真机结果见下方验收记录；实时视图不自动开启新的采集。

## 统一日常入口

底部只有“记录、消息”两个标签。“记录”默认显示时间线，顶部“开始记录”开关控制采集；右上角进入第二页“记录设备”，用双列卡片展示手机、眼镜、手表／手环及电脑。返回记录或系统返回键回到时间线。设备卡片只提供必要连接与授权，没有常规模式、来源或处理设置表单。

本次导航变更保留已保存的采集和处理策略，不重写历史记录，也不因打开设备页开始采集。音频来源仍由唯一采集服务自动选择。旧 home 启动参数统一进入时间线，独立首页、设备标签和重复预览入口已移除。已保存记录仍按原策略处理；眼镜视频和健康能力的验收范围见下文。

## 保存、转写与回看

- `RabiConversationService` 是唯一运行 owner；旧独立本地音频、录像及设备状态 service 已从本轮源码移除。视频由普通 controller 接收，音频统一写 durable spool，健康 controller 服从总许可和允许窗口。
- 新记录冻结 `captureId`、物理来源、Route 与处理策略；切聊天人格不改投排队记录。本地媒体和转写按记录关联，不把当前会话当成历史归属。
- 新转写已按 `captureId` 只读关联；刷新最近 24 小时、最多 200 条、按 `processedAt` 查询的结果；可见实时页有界刷新，历史页手动刷新。缺 ID 不猜归属，不能称完整历史回看。健康目前只统一采集/状态，完整历史仍在 PC。
- `autoResume` 无行为 UI 已移除，仅内部保留 false；开机明确暂停，自动恢复采集尚未实现。
- `local_only` 不提交处理；`transcribe` 要求 PC 支持仅转写且不投递 Agent；能力与 worker 围栏已接入源码，最终验收仍待，无支持时保留 `deferred`，不得降级为 Agent 投递。排队、上传确认、转写完成分别显示。
- 一次视频对应一条记录，内部原始分段按顺序回放。分享导出原始分段，不伪称合并文件。旧 WAV、视频、PCM 队列及已收到转写保留，不改写来源。
- 新记录即使收到传输 ACK，也**不会按旧传输缓存保留期限自动删除**。当前尚未实现完整自动滚动删除，不能称全天容量管理已经完成。未确认/隔离数据不静默删除；空间不足停止接收并报告。

## 状态、隐私与设备边界

开始接收不等于眼镜开播，收到视频才计为真实覆盖。授权、连接、实际数据和 PC 处理分别显示。模式切换、设备断线或写入异常的缺口必须可见，服务存活时长不是录音覆盖。

手机暂停不等于已关闭厂商眼镜相机或手表测量。手动乐奇直播仍需在乐奇结束；PC 健康 Companion 未确认停止时要显示远端未确认。允许窗口及历史隐私区间必须阻止恢复后的迟到健康样本补入。

原生乐奇手动直播保留已有真机证据；自有眼镜自动安装/开播仍需单独验收，不使用 CXR-M。旧录像入口如有兼容调用，只能转到统一 owner，不能恢复旧独立 service。

## 故障与验收

0.3.28-dev（versionCode 31）：140 项 Android 单元测试、assemble 与根 npm run build 通过。真机验证事件点击定位到 21:50:42、录音声波、滚动刷新事件、9 月 8 日跨到 9 月 7 日及返回实时。双指缩放已实现，本轮未做真机多指手势验收。

0.3.27-dev（versionCode 30）：139 项 Android 测试、2 项主题检查与 assemble 通过。手机验证默认时间线、右上角双列设备卡片、返回记录、顶部开始／暂停及停止保存；测试后采集为暂停。

采集使用有界队列、单写、持久来源、原子封口和独立上传。队列恢复不应阻塞主线程；保存失败保留证据，强杀/断电不等于正常保存。预览错误不应中止已正常运行的录像。分享使用 FileProvider 临时读授权，卸载前导出需要保留的文件。

2026-09-16：0.3.25-dev（versionCode 28）通过 Android 137 项测试与 assemble，根 npm run build 通过，已安装到手机。真机完成约 70 秒手机录音并保存；历史 72 秒双分片录像显示正确时长，跳转到 9 秒后画面计时与播放器一致。眼镜已连接，但此轮没有收到眼镜 PCM。0.3.26-dev（versionCode 29）完成 139 项 Android 测试、assemble 和根 npm run build，已安装；真机保存 65 秒录音，实时声波、历史声波、回放进度线、跳转与最右端返回实时通过。测试后采集暂停，处理设置保持仅本地保存、上传暂停。唯一 owner、通知、来源冻结、隐私窗口、仅转写、视频音轨与健康联动以及 24/72 小时长稳均需按新架构验收。

### 历史证据（不是本轮验收）

2026-09-08 旧界面版本：Android 构建与单元测试、互斥和 WAV 内容检查通过；手机完成 80.46 秒录音/后台/有声回放及断网 49.42 秒录音保存。局域网测试源 72 秒推流，预览、跨页录像与两段 60.064/12.014 秒音视频完整解码通过。这些证据保留，但不能证明本轮 owner 重构、眼镜麦克风或自有眼镜开播已验收。更多历史证据见[离线录像](rabilink-offline-recording.md)。

0.3.30-dev（versionCode 33）：140 项 Android 测试、assemble 和根构建通过。真机验证选择录音后布局不移位、点击浮层显隐及播放后自动收起。音视频共用该浮层，本轮浮层交互验收使用录音。

移动时间尺只改变定位，不改变原有播放／暂停、倍速和控件显隐状态；跨记录加载后恢复这些状态。

0.3.31-dev（versionCode 34）已安装。Android 140 项测试和打包通过；真机验证默认隐藏、点击显示、播放中拖动继续播放、跨记录保持播放，以及暂停时拖动保持暂停和控件显示。根构建已执行，本轮被 WebGUI 的既有并行改动类型错误阻断（RoleKnowledgePage.vue、planFeedbackFocus.ts），未修改这些无关文件。

应用更新或进程退出后，若记录意图仍为开启但实际服务已不存在，记录页清理旧运行状态，显示“录音已中断，请重新开启记录”。启动中的服务有短暂等待窗口；没有最近音频时显示“等待声音”，不把旧状态当作正在收音。

0.3.32-dev（versionCode 35）已安装，140 项 Android 测试及打包通过。真机复现旧开关开启但服务不存在，安装修正后显示中断；重新开启后收到持续 PCM、显示实时声波，并出现已保存录音与时间尺绿条。测试结束保持用户开启的采集状态。根构建被无关 WebGUI catalog.ts 重复属性错误阻断。


### 录音事件拆分

0.3.33 开始，新采集音频按独立事件保存边界：有效声音累计至少 1 秒后，低于声音阈值持续 500 毫秒结束事件；最长 60 秒（包括纯静音）。这些默认值参考 PC ASR，手机按 PCM 能量判断停顿，不表示已识别人声。所有 PCM 均保留，不删除静音，不重复预录音。5 秒可靠存储分片与事件是不同层，事件可跨多个分片。

记录右上角的设备页提供“记录设置”：停顿滑条 200–3000 毫秒（步长 50 毫秒）、最长时长滑条 3–120 秒（步长 1 秒），可恢复默认；保存后下一事件生效。事件 ID 写入分片及恢复元数据，重启不重新拆分已有事件；旧记录缺少事件 ID 时沿用原记录。每个新事件可独立回放和分享，点击仍定位时间轴。录音总开关、来源和本地/上传策略保持独立。PC 目前仅返回整次采集关联，缺少可靠事件时间偏移时仅在首个事件标注整次转写，不复制到每个片段。事件拆分本身不启用上传或 ASR。

验证：145 项 Android 单元测试通过，包含停顿/时长边界、PCM 无损拼接、参数下一段生效和分片恢复后的事件 ID。0.3.33-dev（versionCode 36）已安装真机，持续采集产生多个独立事件；点击 38 秒事件后时间轴定位到起点，回放推进且记录继续。设置保存回读为 500/60000，原 local_only 与 uploadEnabled=false 保持。根 npm run build 两次在 Web 构建期间因其他源码变化撤销候选，未完成整体构建发布；不影响本次 Android 构建验收。

0.3.34-dev 将上述参数改为滑条，范围及步长对齐 RabiPC `SpeechServicePage.vue`，默认静音收尾 500 毫秒、最长语音 60 秒。拖动实时显示数值和单位；恢复默认后仍须保存。旧配置逐项限制到新范围并对齐步长，历史事件不变。

滑条版验证：145 项 Android 测试及 APK 构建通过；真机安装 0.3.34-dev（37），拖动保存回读为 1900 毫秒/94 秒，恢复默认保存回读为 500 毫秒/60 秒。根构建仍因并发 Web 源码变化撤销候选，未发布整体版本。


0.3.35-dev（38）纯收音实时预览默认显示金色虚线“转写参考线 0.015”，参考 RabiPC 波形样式，超过阈值的柱条显示绿色。标线与手机事件拆分共用声音阈值；它用于观察声音强弱，不表示已送入 ASR 或完成转写。历史回放不显示当前参考线。记录右上角 → 记录设置可关闭显示，保存后生效；恢复默认会重新开启，录音和分段不受显示开关影响。

验证：145 项 Android 测试、APK 构建和根 npm run build 通过。真机安装 0.3.35-dev，已验证默认标线、关闭隐藏和恢复默认；原 500 毫秒/60 秒参数保留，录音持续。


0.3.36-dev（39）新增“声音阈值”滑条：范围 0.001–0.300、步长 0.001、默认 0.015，对齐 RabiPC 转写阈值的范围。保存后实时参考线及绿色柱条使用新值，分段判断从下一事件生效；恢复默认同时恢复 0.015。它控制手机声音判定，不更改 PC ASR 配置。

阈值滑条验证：146 项 Android 测试及 APK 构建通过。真机拖动保存为 0.108 后参考线同步显示 0.108，恢复默认保存为 0.015，录音继续。根构建两次分别遇到生成文件缺失和 Web 构建锁占用，未完成本轮整体构建。


0.3.37-dev（40）实时音柱改为每 1600 个 PCM 样本生成一柱（16 kHz 下 100 毫秒），不再按回调到达的时间格填充。回调抖动或批量送达不制造空柱；零电平显示灰色细底线，不伪造声音。超过 1 秒未收到音频时清空声波，恢复采集时丢弃旧显示缓存；此改动不更改录音文件。

音柱修正验证：148 项 Android 测试及 APK 构建通过，包含回调分批/抖动一致性、真实断流及静音测试。真机已安装 0.3.37-dev，实时音柱连续显示，保留当前阈值，录音继续。


0.3.38-dev（41）设备中心支持添加多台电脑，每台独立显示。旧连接自动迁移；重复连接更新原卡片。点击卡片验证并切换当前电脑，返回设备中心刷新列表。当前选定一台作为消息和记录处理目标。原单连接配置保留为传输服务的当前目标投影，电脑列表保存各台连接。

多电脑验证：152 项 Android 单元测试及 APK 构建通过。安装时 USB 设备已断开，本轮尚未安装或完成真机多电脑验收；安装前暂停的录音尚未恢复。

0.3.39-dev（42）设备页第一排为返回箭头图标和标题；第二排固定工具栏放置“记录设置”和“添加设备”。下方为可滚动双列设备卡片，不再显示添加电脑卡片。添加设备可选择电脑、眼镜或手表 / 手环。

验证：0.3.39-dev（42）已安装真机，第二排工具栏、添加设备选择、记录设置弹窗与返回图标通过交互检查。152 项 Android 测试、APK 和根构建通过。录音已恢复并出现实时声波。多电脑保存经过单元测试，尚无第二台真实电脑联机验收。

0.3.40-dev（43）设备页关闭按钮移至右上角，使用 24dp × 图标和 48dp 点击区域；关闭只返回记录页，不停止录音。第二排工具栏不变，旧返回箭头资源移除。

关闭按钮验证：0.3.40-dev 已安装，真机点击右上角 × 返回记录页，录音和实时声波继续。Android 测试及 APK 构建通过；根构建受 Manager controlPlaneRoutes.ts 类型错误阻塞，未发布整体版本。

0.3.41-dev（44）设备中心只显示一张电脑卡片。点击后读取当前 RabiLink 服务器的电脑列表并选择切换，服务器确认后更新当前目标。工具栏新增 RabiLink 配置入口；添加设备只保留眼镜和手表 / 手环。一个 RabiLink 可有多台电脑，手机同一时间选择一台。已有保存连接保留用于当前电脑名称及兼容旧登录数据，不再展开成多张卡片。

验证：0.3.41-dev 已安装；真机从 RabiLink 读取两台电脑及当前选择，重新选择当前电脑、RabiLink 配置返回、添加设备仅含眼镜 / 手环均通过交互检查。152 项 Android 测试及 APK 构建通过。根构建因并发 Web 源码变动撤销候选。

0.3.42-dev（45）移除 RabiLink 配置页的电脑下拉框及“使用所选 PC”按钮，电脑选择统一在设备中心电脑卡片中进行。

0.3.43-dev（46）记录设置改为紧凑参数行，参数解释和范围默认隐藏在问号内；保留原滑条范围、步长、保存与恢复默认语义。背景、文字、按钮及滑条使用现有共用主题令牌，关闭图标也跟随浅深色主题。

紧凑版验证：0.3.43-dev 已安装，问号展开及保存通过真机交互检查。152 项 Android 测试及 APK 构建通过；根构建受 Manager 类型错误阻塞。

0.3.44-dev（47）记录设置帮助入口改为 20dp 圆圈问号矢量图标，无方形底框，保留 48dp 点击区域并跟随主题文字颜色。

圆形帮助图标验证：0.3.44-dev 已安装，真机确认无方形底框、问号展开说明正常；152 项 Android 测试及 APK 构建通过，录音已恢复。根构建失败，未发布整体版本。

## ASR 电脑优先级（0.3.45-dev）

设备页工具栏增加“ASR 设置”。列表仅包含当前 RabiLink 应用下声明 ASR 能力的电脑；长按右侧手柄拖动排序，保存到服务器。转录事件优先使用靠前的在线电脑，连接或转录失败后尝试下一台，全部失败保留本地音频重试。消息处理电脑的选择独立。

RabiLink 用于发现与鉴权握手，音频连接依次尝试局域网、P2P、加密服务器中转。会话复用；已固定身份的电脑可直接局域网重连。内存缓存电脑列表 30 秒，刷新失败保留已知列表；应用重启后的首次发现仍需服务器。手机新转录事件在完整分段后处理，先持久保存文字再确认音频分片；崩溃恢复使用已有文字，避免重复确认。语音处理开关关闭时不能自动建立新的语音授权。

本轮验收：Android 157 项测试通过。真机分别验证 LAN、P2P 和服务器中转上的 WAV 转录；中转由测试显式选用，不改产品自动选择顺序。独立测试事件验证转写持久化和分片确认，不读取已有录音。当前只有一台在线 ASR 电脑，多台真实电脑间的故障切换尚未实测；排序、离线回退和应用隔离由自动化测试覆盖。首次握手后的公钥按规范 PEM 保存，Android 与 Node 使用相同的签名文本编码。

新转录事件绑定 RabiLink 应用，消息电脑切换不影响 ASR；切换应用后旧事件不会自动迁移。旧的 Agent 处理策略保留原目标绑定和上传合同，防止历史消息被重新投递。


## 录音卡片转录（0.3.46-dev）

保存非空 ASR 列表后，新录音启用转录，已有的本地事件加入补转队列；Agent 记录和其他 RabiLink 应用的转录绑定不迁移。后台保留待转录、转录中和等待重试状态；当前回看卡片只显示有文字的 ASR 事件，回看时自动刷新结果，不改变播放状态。

补转包含旧版未分事件的本地音频，每组至多约一分钟，后台分批入队。历史确认恢复一次索引 PCM 文件，避免逐条扫描目录。

全天记录已移除旧版无 capture 生命周期的音频入口及 capture 级 PC 转写查询。手机启动后把无记录归属的旧上传队列从调度索引退役，原文件不作为新任务发送；转录卡片只读取分段持久结果。

全天记录不再执行旧上传缓存的确认后清理；确认日志维护最多每分钟扫描一次，接近容量上限时立即检查。此调整避免批量补转与麦克风写入争用长时间文件锁。

0.3.46-dev 真机验收：ASR 设置保存和回读通过，卡片已显示实际分段文字与“未识别到语音”；后台补转期间持续收音通过。156 项 Android 测试及 APK 构建通过。根构建受 `dshHttpAuth.ts` 类型错误阻挡，本轮未发布整体 PC 包。


## 存储管理（0.3.47-dev）

设备页工具栏第二排增加“存储管理”。显示本机记录文件总大小，分为录音、录像、转录文字、记录索引与日志、回看与导出缓存，并显示手机可用空间。采用十进制 KB/MB/GB，1 GB = 1000 MB。后台扫描记录目录，正在写入的文件也计入；点击刷新重新统计，未读取的文件会明确提示统计不完整。此入口不修改或删除文件。

0.3.47-dev 验收：158 项 Android 单元测试、APK 与根构建通过；真机从时间线进入设备页，触摸打开存储统计、总量回读及刷新通过。

## 资源缓存服务（0.3.48-dev）

电脑 WebGUI 的“设置 → 资源缓存服务”配置绝对保存路径，保存时检查可写性。新资源使用新路径，旧资源保留原位置索引。电脑文件承担持久保存职责，不自动淘汰。

手机“存储管理”独立启用电脑缓存并设置本地保留 0–168 小时（0 表示电脑保存后清理），默认关闭、时长默认 24 小时。到期只清理电脑已保存并再次校验成功的媒体；未完成处理的录音、未完成音轨派生的录像继续保留。时间线及转录保留，回看时显示缓冲提示并下载已校验资源到手机缓存。电脑离线且本地无缓存时显示失败，原索引保留。回看缓存访问时清理超过 24 小时未使用的文件。

资源使用独立的 resources 授权，不要求启用 ASR；沿用 RabiLink 引导鉴权及 LAN → P2P → Relay。按最大 1 MiB 分块持久保存，服务端完成 fsync、原子替换和设备隔离索引后才返回回执。保存路径变化与当前选择电脑变化不会重写旧资源的归属。

当前回看先下载完整片段再播放，较大录像的首次缓冲时间较长。0.3.48-dev 验收：158 项 Android 单元测试、17 项后端测试及完整构建通过；真机通过 LAN 验证测试资源上传、本地测试副本删除后的下载、损坏缓存重新下载，以及存储页总量和刷新。电脑设置接口保存回读与运行资源版本核对通过；浏览器工具连接中断，WebGUI 视觉验收未完成。

## 记录页屏幕布局（0.3.49-dev）

整宽 16:9 预览位于顶部，取消独立标题栏。点击预览显示或隐藏记录开关、设备入口、运行状态、当前时间和播放控制；细刻度时间轴及轨道图例叠放在预览底部，保留拖动和双指缩放。所有原有信息保留在紧凑工具栏或控制层中。控制层不改变页面高度；拖动时间轴保留播放状态。

顶部控制层使用透明背景，左上角仅显示实时或回看时间；省略重复的暂停和收音状态文案。

## 2026-09-20 回看体验更新

本轮源码与调试 APK：700dp 及以上宽度使用 60/40 左右布局，左侧预览下方常显时间轴，右侧为来源筛选和事件列表；较窄竖屏保持上下布局。原生 ListView 复用可见卡片，列表滚动和时间轴联动。刷新保留事件 ID、顶部偏移和现有播放器，失败保留已有内容。播放时只更新选中样式，不重建整表。

可见页只在采集信号、状态或音频目录变更时合并刷新；手动刷新仍可用。录像先按会话时间排除窗外目录，时长按路径、修改时间和文件长度缓存在最多 2048 项的页面内存中；关闭页面后重建。缩略图使用独立的有界任务队列，不能阻塞记录查询或挤掉播放任务。原始媒体与元数据保持不变，没有新增删除、迁移或归档。

这些优化不等于完成持久化索引分页：音频元数据仍沿用原有缓存和目录枚举，视频清单仍需枚举。真机已安装调试 APK，并通过竖屏比例、控制层切换不改变布局、横屏左右布局和时间轴完整可见检查；横屏预览高度受可用空间限制，避免挤掉时间轴。当前测试范围无已保存事件，真实事件点击、刷新保位和长时录制尚未覆盖，不能由 APK 构建或单元测试替代。


回看只显示成功转录且有非空文字的 **ASR 事件**；纯录音仍保存，但不显示在事件列表和历史时间轴。事件沿用原始采集时间及稳定 ID，不使用转录完成时间。筛选、分页和上一个/下一个事件均在计页前排除隐藏录音。移动端回到实时仍显示麦克风波形。移动端日期、实时、刷新工具栏增加上一个/下一个事件，事件类型（ASR/录像）与设备来源独立筛选。
