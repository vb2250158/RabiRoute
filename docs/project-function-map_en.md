<!-- docs-language-switch -->
<div align="center">
English | <a href="./project-function-map.md">简体中文</a>
</div>
<!-- /docs-language-switch -->

# RabiRoute Project Function Map

> Status: current fact map. Manager has 29 independent built-in plugin packages. External-system acceptance still follows [Current Capabilities](current-capabilities_en.md).

Use this page to locate the owner of a behavior before editing code. RabiRoute is the triage/dispatch layer: endpoints bring messages in, routing chooses what should happen, handlers do the work, and Outbox controls how results return.

RibiWebGUI `/#/docs` is now the task-based User Guide backed by `docs/user-guide/`. This developer fact map remains available through deeper-reading links and the repository documentation index.

## How to use this map

- To find the owner of a capability, start with **Current function index**.
- To design UI, read **Boundary rules** and the function row's source, consumer, timing, and side effects.
- To change code, use **Layer map** and **Change-entry map**.
- To diagnose runtime behavior, use **Runtime data**.
- To confirm maturity, check [Current Capabilities](current-capabilities_en.md) first.

## One-sentence positioning

RabiRoute is a message gateway and policy router. It owns message ingress, event records, route decisions, context packaging, handler delivery, outbound approval, and observable status; handlers own task execution, tool use, and their sessions.

## Layer map

| Layer | Owns | Must not own | Main code |
| --- | --- | --- | --- |
| Message adapter | Protocol parsing, normalization, adapter logs, health | Handler prompt or direct policy bypass | `src/adapters/*` |
| Event/history stores | Protocol/audit JSONL plus the persona-scoped bidirectional conversation ledger | Route or business decisions, or using archives as automatic context | `src/history.ts`, `src/messageContextStore.ts` |
| RouteDecision | Match rules inside one route profile | Role selection, memory reads, handler delivery | `src/routing/routeDecision.ts` |
| AgentPacket | Structured `messageSource`, unwrapped `content`, and final handler `message`; final bodies start with `[消息源]`, then `[消息内容]` | Route matching or platform send | `src/shared/rabiMessage.ts`, `src/routing/agentPacket.ts` |
| Handler adapter | Deliver a packet to the Route's selected Primary Agent | Platform route semantics, external output policy, or fallback to another Agent | `src/agentAdapters/*`, runtime modules |
| Outbox / Send | Validate the calling Agent type/session, explicit Route, channel, target parameters, payload, and policy before sending, then retain traceable receipts | Handler reasoning or destination inference from source context | `src/agentSend.ts`, `src/outbox.ts` |
| Manager | Configuration, processes, scans, APIs, shared services | Platform-specific live parsing | `src/manager.ts`, `src/manager/*` |
| Plugin platform | One Profile, independent packages, `provides/requires/optional` graph, permissions, generations, Plugin Catalog, and effect disposal | Add concrete plugin-ID branches to a host or bypass the SDK to mutate host state | `src/plugin-kernel/*`, `plugins/contracts/plugin-sdk/*`, `plugins/builtin/*`, `plugins/profiles/desktop.json`, `src/manager/pluginCatalogRoutes.ts` |
| WebGUI / Desktop minimal hosts | Connect to Manager, consume the contribution catalog, render safely, and maintain window/page shells plus fixed recovery entries | Maintain a second catalog of pages, settings, commands, navigation, status, or lifecycle entries | `ribiwebgui/src/*`, `desktop/tray-task-window/*`, `src/manager/pluginCatalogRoutes.ts` |
| Role knowledge | Plans, memories, skills, recall, validation, Manager-owned presentation ordering, and exact bound-task status reads | Decide whether an event matches a route or infer Codex work state from plan lifecycle | `src/roleKnowledge.ts`, `src/roleKnowledgePresentation.ts`, `src/manager/planAgentStatus.ts` |

## Manager background-plugin lifecycle

The catalog contains 29 built-in Manager instances. Production startup uses only `startManager()` to mount host services, read schema/profile v2, and let Plugin Kernel resolve the 29 packages through `provides`, `requires`, `optional`, execution modes, and `readyRequires`. Missing required capabilities produce `waiting_dependency`; dependency revisions include direct and transitive providers, so an upstream revision or active-state change moves real consumers into the generation switch. Duplicate providers reject reconciliation. Shutdown follows reverse dependency order—consumers before providers—and process leases reclaim long-lived children.

| Group | Instances | Main responsibility |
| --- | --- | --- |
| Host and presentation | `manager:core`, `manager:desktop`, `manager:desktop-pet`, `manager:diagnostics` | WebGUI LAN configuration entry, Desktop lifecycle/settings, bounded desktop-pet pack/binding APIs, Manager metadata, and Gateway diagnostics |
| Persona and runtime observation | `manager:persona`, `manager:speech`, `manager:performance` | Persona/sync/language style, speech services, and performance collection/query |
| Gateway and external connections | `manager:gateway-runtime`, `manager:bilibili-history`, `manager:rabilink-relay`, `manager:napcat-control`, `manager:napcat-supervisor`, `manager:remote-agent`, `manager:yeyu-gamer` | Gateway processes, Bilibili history bridge, Relay/LAN, NapCat control/startup checks, Remote Agent Hub, and the local YeYu Gamer typed-API facade |
| Route and Adapter control | `manager:route-control`, `manager:message-adapter-control`, `manager:agent-adapter-catalog`, `manager:agent-state-control`, `manager:agent-thread-control`, `manager:agent-communication` | Route/Gateway APIs, message-adapter control, and Agent catalog/state/thread/communication APIs |
| Agent provider control | `manager:copilot-control`, `manager:astrbot-control`, `manager:marvis-control` | Installation, login, process, or connection control |
| Background business services | `manager:memory-consolidation`, `manager:fennenote-output`, `manager:message-processing-control`, `manager:message-processing-automation`, `manager:plan-feedback-delivery` | Memory consolidation, FenneNote output, message-processing board/reminders, and plan-feedback recovery delivery |

Seven instances publish presentation contributions; twenty-one have no presentation contribution but still own real lifecycle work. Production business routes use stable `routeId` values and real `exact` or `prefix` matchers; duplicate IDs and, when methods overlap, `exact/exact`, `exact/prefix`, and `prefix/prefix` path conflicts are rejected. Trusted WebGUI and Desktop contracts are bound to `pluginId + instanceId`, so cross-plugin catalog references fail closed. The `manager:desktop` `settings-section` owns system-selection, system-screenshot, clipboard-image hotkey, and login-startup settings; active contributions also control persona/plan/memory/project/runtime directories and manual trigger. If the catalog is unavailable or refresh fails, stale contributions are cleared, Desktop stops its system listeners, and only the fixed WebGUI recovery entry remains.

Each plugin performs ordered teardown in one disposer:

```text
unregister routes
→ stop accepting new requests
→ drain accepted requests
→ stop plugin-owned workers/processes/timers/sockets/services
→ await resource exit
```

Cordis runs multiple disposers in one Fiber concurrently through `Promise.all(...)`, so ordered teardown cannot be split across several `ctx.effect()` calls. Current Manager hooks keep the critical sequence in one disposer; `ManagerPluginRequestTracker.trackOperation()` waits for business Promises beyond the response lifetime. Manager shutdown and startup-failure cleanup run `managerPluginKernel.dispose() -> managerSharedResourcesRuntime.unmount() -> managerCordisRoot.dispose()` in sequence. Shared-resource stop cancels queued/shared requests, flushes writes, terminates Workers, and waits for exit. If one shared resource fails to stop, cleanup continues for the others and rethrows the first error afterward. RabiLink aborts and then waits for its active run, LAN listener, sockets, Relay, SSE, WebGUI, and speech drains. A second `stop()` cancels a restart queued during shutdown, and both the configuration watcher and Rabi identity PATCH await asynchronous Relay synchronization. Batch reconciliation deactivates in reverse order before dependency-ordered activation.

The central HTTP chain is limited to LAN authentication, the read-only write gate, plugin route dispatch, Manager SSE, plugin catalog/reconciliation, static assets, JSON 404 for control paths, and WebGUI HTML fallback for all other paths. `/manager` and every `/manager/*` path remain control-plane responses.

Trusted built-in plugins run in the Manager process. Unknown, untrusted, or high-risk extensions use a separate process and a least-capability protocol as additional RabiRoute hardening. Ordinary DSH plugins also run in the main process; Cordis scope/isolate is not a process sandbox.

## Current function index

| Function | Maturity | Source / trigger | Side effects | API / UI | Main code |
| --- | --- | --- | --- | --- | --- |
| NapCat inbound/outbound | verified; one NapCat listener per Route uses Fiber lifecycle | OneBot WS/HTTP | message logs and external QQ sends; activation owns the Route's single listener, and disposal terminates its client, releases its port, and records `disabled`. Explicit recovery still preserves the live account owner, blocks duplicate startup, and distinguishes expired quick login from QR-required authentication | route config, message scan, and **Open NapCat** | `src/adapters/napcatAdapter.ts`, `src/runtime/messageAdapterRuntime.ts`, `src/napcat.ts`, `src/outbox.ts`, `src/messageEndpoints/napcatManager.ts` |
| Persona automation | verified locally | persona `automationRules`; legacy `notificationRules` migrate on read | message or schedule triggers can notify the Agent or run a constrained persona script; the two result types remain separate | Persona page dynamic **When a message arrives / Scheduled tasks** editor | `src/automation/personaAutomationRuntime.ts`, `src/forwarding.ts`, `src/adapters/heartbeatAdapter.ts`, config repository |
| Scheduled-task runtime | verified; Fiber-owned timer lifecycle | `automationRules[].trigger.schedule` | Agent actions use heartbeat logs without recent chat history; script actions use a separate local execution ledger and cannot overlap the same rule. Fiber disposal clears future timers, rejects stale callback rearming, and records `disabled`; work that already started continues | Route scheduled-task input plus separate local script permission | heartbeat adapter, Message Adapter runtime, automation runtime, forwarding, and Message Agent pool |
| Manual trigger | verified | Manager/WebGUI action | manual-trigger log and handler turn | Manager control plane | `src/manualTrigger.ts`, `src/manager/controlPlaneRoutes.ts` |
| Interface and custom themes | verified locally | `theme`, `webTheme`, and `customThemes` in `data/desktop/settings.json` | writes host desktop settings and refreshes WebGUI CSS/Vuetify plus Windows Qt presentation; it does not change business data | WebGUI Settings → Interface theme | `src/shared/interfaceThemeContract.ts`, `ribiwebgui/src/interfaceTheme.ts`, `desktop/tray-task-window/rabiroute_tray/themes/` |
| Work-ended events and local announcements | available; real Codex / DSH producers pending | `work-events/events/YYYY-MM-DD.jsonl`, task-announcement settings, and metadata ledger | redacts and retains the work event, then applies source policy before synthesis, playback, and receipt recording | `/api/work-events/ended`, `/api/speech/task-completion/*` | `src/manager/workEndEvents.ts`, `src/manager/taskCompletionAnnouncements.ts` |
| YeYu desktop pet | available; Windows device acceptance pending | `io.rabiroute.manager.desktop-pet`, `data/roles/YeYu/desktop-pet/`, and Manager desktop settings | the plugin generation owns bounded pack/binding APIs; Desktop consumes matching work-ended events without mutating task or persona documents | `/api/desktop-pet/roles/YeYu*` | `plugins/builtin/io.rabiroute.manager.desktop-pet/`, `src/manager/desktopPetRoutes.ts`, `desktop/tray-task-window/rabiroute_tray/desktop_pet_*` |
| YeYu Gamer Manager | experimental; installed-machine acceptance pending | `io.rabiroute.manager.yeyu-gamer` Profile config, fixed loopback 8877 API, and a separate `rabiroute.token` | reads health/meta/snapshot/capabilities and creates only `mode: "plan"` work items; it cannot claim work or invoke capabilities | `/api/agent/yeyu-gamer/*` | `plugins/builtin/io.rabiroute.manager.yeyu-gamer/`, `src/integrations/yeyuGamer/` |
| Role panel | verified | Qt/Manager local message | role timeline and handler turn/reply | Manager/Qt | role-panel modules and Outbox |
| Persona directory and cross-persona messaging | automated contract verified; real two-persona Desktop acceptance pending | shared persona catalog, Manager runtime registry, Route-and-persona-bound capabilities, and durable receipts | directory reads; POST uses the shared role-panel service, records success only after handler acceptance, and does not repeat one `deliveryId` | `/api/personas*`, `/api/personas/messages/receipts/:deliveryId` | `src/manager/personaCatalog.ts`, `personaMessageAuthority.ts`, `personaMessagingRoutes.ts`, `rolePanelDelivery.ts` |
| Idempotent explicit Agent send | current | `POST /api/agent/send` with required stable `deliveryId`, `sender.agentType + sender.sessionId`, exact `routeId`, `channel`, channel-specific `params`, and `payload` | persists a reservation before Outbox, executes the same ID/request once across concurrency and restart recovery, returns conflicts for changed senders or payloads, preserves the calling Agent session in completed receipts, supports lookup by channel plus `sentMessageId`, never auto-replays uncertain work, and never derives the destination from source context; NapCat group sends require an explicit `replyToMessageId`, using a real ID to quote or `""` to select no quote; quoted image messages require ordered `replyImageDescriptions`, and a successful send creates same-name Markdown descriptions beside the managed images | `POST /api/agent/send`, `GET /api/agent/send/receipts/:deliveryId`, `GET /api/agent/send/traces` | `src/agentSend.ts`, `src/replyImageDescriptions.ts`, `src/manager/agentSendIdempotency.ts`, `src/outbox.ts` |
| RabiRoute Windows Host | current | the dynamic Manager URL published by Host READY, same-generation runtime identities, and packaged assets | `RabiRouteHost.exe` is the sole lifecycle owner and directly owns the Manager and Tray child processes within one application generation. Tray neither starts nor scans for Manager; it consumes only the current identity and dynamic endpoint published by Host READY. WebGUI renders Manager DTOs asynchronously and never reads runtime `data/` directly. After WebGUI Settings enables the selected-text menu, Tray supports mouse dragging and `Shift` keyboard selection. Normal apps use UI Automation for text and selection bounds; Unity sends guarded temporary copy input and restores the clipboard only when that selection is unavailable. The bar is horizontally centered on the selection, above upward drags and below downward or same-line drags; without a system caret it uses the most recent click in the same window. Actions still require a click; turning off Selected-text reading leaves only Send to | `RabiRouteHost.exe --command status --json`, Host READY Manager URL, `/gateways?summary=1`, `/api/roles/:roleId/*` | `desktop/windows-host/`, `desktop/tray-task-window/`, `scripts/build-windows-host.ps1` |
| Remote Agent | experimental | discovered bridge and explicit task | connection/task/event/file runtime data | `/api/remote-agent/*` | `src/messageEndpoints/remoteAgentManager.ts` |
| Generic webhook | experimental; Fiber-owned lifecycle implemented | HTTP POST | adapter/event logs and handler turn; startup waits for the listener, failed activation rolls back, and Fiber disposal closes the port and records `disabled` | configured port/path | `src/adapters/webhookAdapter.ts`, `src/runtime/messageAdapterRuntime.ts`, webhook scans |
| FenneNote compatibility | retired/hidden | legacy transcript webhook and output bridge | compatibility logs only when an old route still references it | no longer offered by the add-adapter UI | webhook-like adapter, Outbox |
| WeCom | experimental | smart-bot WebSocket | message/status logs and group send | scan and route config | `src/adapters/wecomAdapter.ts`, `src/wecom.ts`, `src/messageEndpoints/wecomManager.ts` |
| Personal Weixin | experimental prototype | OpenClaw/iLink QR login, long polling, context tokens | message log and source-session text reply | route config and adapter status | `src/adapters/weixinAdapter.ts`, `src/weixinOpenClaw.ts` |
| Glasses endpoint through RabiLink | experimental; legacy internal key `rabilink` | glasses observations and downlink | conversation ledger, compatibility logs, device output | route message-adapter entry | RabiLink adapter/worker/Outbox |
| Wearable health endpoint | experimental; internal key `wearable`; Manager-internal entry | structured Relay observations from Health Connect or a trusted ADB bridge | role-scoped health timeline, Manager queries, `wearable_health_alert`; Gateway creates no standalone Wearable Adapter, and the RabiLink worker passes health tasks to the health-rule modules | compatible Route key and `/api/roles/:roleId/health/*` | `src/adapters/rabilinkRelayWorker.ts`, `src/wearableHealth.ts`, `src/manager/wearableHealthRoute.ts` |
| RabiLink system transport | experimental; internal contracts tested | global Relay configuration, remote WebGUI HTTP/SSE, speech/observation queues, and downstream device receipts | Manager subscribes to `/api/rabilink/events` SSE and immediately claims only after availability events; it separately subscribes to local Manager `/api/events` and forwards each event to authenticated remote pages. Remote APIs, attachments, and downloads use the constrained queue, while media preserves Range/206. Mobile performs one cursor query for gap recovery. Explicit-target messages do not TTL-expire before `delivered`; Relay persists `delivered/played/playback_failed` and emits `outbox_receipt` without guessing playback. Transport does not select a Route or own an Agent | Rabi instance on Console | `src/manager/rabiLinkRelayRuntime.ts`, `scripts/rabilink-event-hub.mjs`, Relay server |
| RabiLink phone edge hub | first contract plus playback-receipt loop | application token, stable device identity, per-device cursor, target/presentation envelope, phone-private reliable queues | network/SSE events wake one cursor catch-up query. Known-offline SSE/reliable work waits for the system connectivity event; a five-minute OS-connectivity check runs only while already offline to cover vendor callback loss. Forty-five seconds without an SSE keepalive rebuilds only the half-open transport, and only an available network with a server failure uses 1–30 second backoff. Text/media/receipts replay after offline periods while PCM uses bounded live catch-up. The phone owns transport, crash deduplication, durable restore intent, and peripheral fan-out, not the Agent or ledger; neither the offline safety check nor the SSE stall deadline reads Relay business messages. Started transport restores after restart, while explicit Stop disables restore. `delivered` means presentation only; phone and glasses emit `played` only after their own AudioTrack marker. Glasses accept PCM only after capture pause is confirmed and fail unfinished playback on destruction. A full reliable queue rejects new work rather than deleting old work | `/api/rabilink/devices/input`, `/api/rabilink/devices/messages`, `/api/rabilink/devices/message-receipts`, Android SDK/app | Relay server, `packages/android-sdk/rabiroute-sdk/`, `apps/rabilink-android/` |
| Active-intelligence physical acceptance status | implemented, fail-closed | real-person voiceprint manifest/formal report, two-PC sync evidence, Android soak, Rokid real-device summary, operator physical observations | sanitized aggregate JSON and CI/operator terminal status | explicit one-shot Agent/operator command; starts no test and polls no device | each of four domains reports `missing/partial/passed/stale/invalid`; automation is prerequisite only, synthetic voice cannot impersonate formal evidence, and aggregate output retains hashes, times, and check results only | `npm run check:active-intelligence:physical` | `scripts/check-active-intelligence-physical-acceptance.mjs` | `docs/rabilink-active-intelligence-requirements_en.md` |
| Multi-PC persona synchronization | experimental | local persona folders, rebuildable persistent manifest index, durable pending-reconciliation scope, same-application peer discovery, per-peer common hashes, archives, unresolved evidence, and resolution records | one startup reconciliation followed by file-event index maintenance; `PersonaSyncAutoReconciler` performs one manifest catch-up on file changes, peer availability, or Relay reconnection, using LAN first and restricted Relay fallback. The persona page and Agent/API retain explicit sync and conflict operations; P2P never exposes the full Manager/WebGUI or diagnostic/conflict control | file events rehash one path and mark that persona pending; Relay `ready`/peer events trigger a full catch-up; `/api/persona-sync/sync` remains an immediate command; AgentPacket injects the API contract only when the routed task explicitly asks the Agent to handle synchronization | pending scope persists in `data/persona-sync/auto-sync-state.json`; disconnects/restarts cannot erase it. Offline peers wait for events and online failures use bounded backoff, not fixed polling. JSONL union merge, common-base fast-forward, deletion conflicts, evidence, and audit remain owned by the existing services. WebGUI supports discovery, current-persona sync, evidence preview, and `keep_local/use_remote`; `use_merged` remains an Agent/API action. Voice semantic conflicts return to persona classification for explicit convergence | `/api/persona-sync/*`, `/api/rabilink/peers`, `/api/rabilink/persona-sync/proxy` | `src/personaSync.ts`, `src/personaSyncManifestIndex.ts`, `src/personaSyncCoordinator.ts`, `src/personaSyncAutoReconciler.ts`, `src/manager/personaSyncRoutes.ts`, `src/manager/personaSyncLanServer.ts`, `ribiwebgui/src/components/PersonaSyncCard.vue`, Relay server, `docs/persona-data-sync_en.md` |
| RabiSpeech TTS / ASR | experimental | RabiSpeech configuration, host voiceprint embeddings, host-wide `data/speech/messages/YYYY-MM-DD.jsonl`, persona `voice-transcripts.jsonl` and `conversation/current.jsonl`, persona voice profile and cache | Android and ordinary remote clients transport PCM only. One ASR record is stored once; host/ordinary remote sound cards use `speech`, Android streams use `rabilink` after PC-side VAD/segmentation/ASR/voiceprint, and each persona keeps complete turns. The page-wide WebGUI switch safely starts/stops this Windows workspace through Manager, reveals runtime controls only after real health, and hides them after shutdown. The ASR page has a separate persisted host-wide streaming switch: turning it on continuously records and transcribes, while Route subscriptions only control result delivery. With no subscriptions the host still stores its record; turning the switch off leaves manual uploaded-audio transcription available. `/v1/events` is proxied through Manager `/api/speech/events` for microphone, playback, audio-stream, and successfully persisted `records_changed` events. Android chunks are contiguous; stable `chunkId` values provide cross-stream idempotency after a lost ACK, system-network/SSE events wake continuation, and bounded buffering prevents permanent recovery lag. Each accepted chunk rearms a one-shot 15-second expiry. The host emits only opaque evidence, stores no names, and decides neither identity nor user relationship. `npm run check:speech-ingress-separation` verifies the isolated built host store, endpoint split, persona histories, and stable mobile reply device | `/#/speech`, `/v1/events`, `/v1/models`, `/v1/records`, `/v1/audio-streams/rabilink/*`, `/api/speech/events`, `/api/speech/runtime/start|stop`, `/api/speech/selection-reader/settings`, `/api/speech/*` | `plugin-adapters/rabi-speech/`, `desktop/tray-task-window/rabiroute_tray/system_selection.py`, `src/manager/selectionSpeechSettings.ts`, `src/speechIngressStore.ts`, `src/manager/speechRuntimeControl.ts`, `src/manager/speechControl.ts`, `src/routing/speechIngressForwarding.ts`, `src/acceptance/speechIngressSeparation.ts` |
| Local speech model management | experimental | fixed `plugin-adapters/rabi-speech/model-catalog.json` allowlist, plugin-private `.deps`, external model root, and install manifest | the standard install includes neither speech dependencies nor models; users prepare the host-wide core speech environment and then download individual TTS, ASR, or speaker weights | Manager serializes repository-owned installer scripts, accepts only fixed model aliases, publishes job changes through `/api/events`, performs one catch-up snapshot on reconnect, and returns no private absolute paths | downloaded confirms files and manifest only; it does not prove an isolated runtime, real inference, waveform, or device acceptance. Licensed ONNX-VITS packages remain manual imports | `/#/models`, `GET /api/speech/model-management`, `POST /api/speech/model-management/runtime/install`, `POST /api/speech/model-management/models/:alias/install` | `src/manager/speechModelManager.ts`, `src/shared/speechModelManagement.ts`, `ribiwebgui/src/pages/ModelManagementPage.vue`, `ribiwebgui/src/speech/speechModelManagementClient.ts` | `docs/local-speech-model-downloads_en.md` |
| Voice endpoint accounts (compatibility data) | available inside the Identity relations page | append-only persona `voice/voice-identities.jsonl`, using compatibility key `sourceHostId + voiceprintId`; conceptually this is the speech platform plus processing-host/recognition-model namespace plus voiceprint ID | the bound persona interprets its own voice accounts; one recording may reference several accounts. Display name, relationship, optional `isUser`, aliases, and notes remain append-only and conflict-aware. The host/RabiSpeech/Route never write persona conclusions. The summary-only view derives classification duration, coverage, and unresolved voiceprints without transcript text. **Mark the next recording** highlights candidates only. Until unified identity-data migration is complete, the compatibility API remains authoritative | WebGUI **Identity relations → Voice endpoint accounts**, `GET/PUT /api/roles/:roleId/voice-identities`, `GET /api/roles/:roleId/voice-transcripts`, AgentPacket `voiceIdentitiesPath` | `src/personaVoiceIdentities.ts`, `src/personaVoiceTranscriptView.ts`, `src/manager/personaVoiceTranscriptRoutes.ts`, `ribiwebgui/src/persona/personaVoiceIdentityClient.ts`, `ribiwebgui/src/persona/personaVoiceConfirmation.ts`, `ribiwebgui/src/pages/PersonaTemplatePage.vue`, `src/routing/agentCapabilityHints.ts` | `docs/rabispeech-plugin_en.md`, `docs/user-guide/personas-and-rules_en.md` |
| Persona identity relations | available, with automatic unfamiliar-account candidates, append-only events, sync-conflict protection, and reviewable situation records | persona `identity-relations/events.jsonl` and `conversation/situations/*.json`; accounts use `platform + endpointIdentityNamespace + senderStableId` and link to participants and relation cards | the persona bound to the delivered message | a real delivery from an unfamiliar stable account automatically creates a “getting to know” candidate; later deliveries reuse it, and the processing Agent appends candidate observations only when new clues appear. The Persona page reviews, confirms, and corrects participants, account mappings, and relation cards, then reviews recent situation records without chat text | a nickname, group privilege, Route, keyword, or one utterance never confirms a person. Preview does not write identity data. Candidates and conflicts cannot support naming, authorization, or project attribution, and the observation API cannot write `confirmed` or overwrite conflicts. A scoped project relation is only a discussion lead. Multi-PC JSONL union retains concurrent heads; one complete correction replaces every current head | Persona page, `GET/PUT /api/roles/:roleId/identity-relations`, `POST /api/roles/:roleId/identity-relations/observations`, `GET /api/roles/:roleId/conversation-situations`, AgentPacket `[Identity relations]` and `[Situation record]`, `semanticConflicts` | `src/identityRelations.ts`, `src/routing/identityContext.ts`, `src/routing/conversationSituation.ts`, `src/conversationSituationStore.ts`, `src/manager/controlPlaneRoutes.ts`, `ribiwebgui/src/components/PersonaIdentityRelationsCard.vue` | `docs/agent-context-injection_en.md`, `docs/plan-and-memory-model_en.md`, `docs/persona-data-sync_en.md` |
| Rabi LAN Voice Client | experimental | private RabiSpeech `remote_audio` settings, dedicated stream token, and current audio-stream selection | meeting-room PC acts only as a remote microphone/speaker; its standalone GUI manages host connection, device selection, live level, and capture/playback state while host VAD, segmentation, ASR, TTS, FIFO, and Route broadcast stay unchanged | client GUI or `--headless`; Speech Service **Audio stream type** selector | `plugin-adapters/rabi-speech/rabispeech/remote_audio.py`, `desktop/rabi-voice-client/` |
| Speech push policy | current | Route `speechPushMode`; persona `speechTriggerKeywords` | `hot` delivers every completed ASR segment immediately; `keyword` records every segment but wakes the Agent only on a persona-name/wake-keyword match; an empty list never falls back to hot | Route Hot delivery switch; persona keyword editor | `src/routing/speechPushPolicy.ts`, `src/index.ts` |
| Per-endpoint recent context | current | persona `recentMessageLimits` for ordinary logical endpoints | `0–200`, default `12`; `0` disables auto-injection only. Inbound and outbound records share the budget within the current persona, logical endpoint, and conversation. Heartbeat short-circuits before reading the ledger and always uses `0` | persona sliders plus exact numeric inputs; Heartbeat is not shown | shared config model and AgentPacket |
| Persona avatar | current | `personaConfig.json.avatar` referencing a PNG/JPEG/WebP/GIF inside the persona directory | Manager writes a content-addressed image and atomically switches the config; both WebGUI and Qt consume the constrained Manager API, and Qt does not read persona files directly | persona page, Quick Setup, Route overview, speech page, Qt role panel | `src/personaAvatar.ts`, `src/manager/personaAvatarRoutes.ts`, `ribiwebgui/src/components/PersonaAvatar.vue` |
| Persona conversation ledger | current | `data/roles/<RoleId>/conversation/current.jsonl` plus `archive/<n>~<m>.jsonl` and `index.json` | no entry-count cap on current; an archive check triggered by data older than 72 hours moves the complete contiguous prefix older than 24 hours; automatic context never reads archives | injected paths in AgentPacket | `src/messageContextStore.ts` |
| Codex handler | verified | AgentPacket | matched ordinary messages try `steer` on the active turn and otherwise `start`; Heartbeat may separately skip while busy, and speech may separately use keyword wake policy | route handler config | `src/codexRuntime.ts`, `src/codexDesktopBridge.ts` |
| DSH handler | experimental | Route `dshSessionId`, `dshSessionName`, `dshCwd`, `dshBaseUrl`, and the DSH Workspace registry | DSH apiproxy `workspace.create`, `session.create`, and `session.prompt` | when save or delivery must resolve a DSH session | continues the exact session while ID and workspace match, and creates idempotently only after zero matches. Before creation it registers or reuses the DSH Workspace for `dshCwd` and passes `workspaceId` to `session.create`, so new Primary Persona, Message Agent, Plan Secretary, and Memory Consolidation sessions appear in the matching group; failures never switch handlers | Route handler configuration; WebGUI scans API address, workspace, then session | `src/dshSessionBridge.ts`, `src/agentAdapters/agentAdapter.ts`, `ribiwebgui/src/pages/RouteConfigPage.vue` | `docs/configuration_en.md` |
| Rabi Codex Context plugin | 0.4 unified-context and completion-Hook version | real Codex session ID, explicit RoleId binding, Manager-owned role configuration, and plan `taskBinding` | full entry context, deduplicated Pre/Post reasoning deltas, plus thin `Stop` forwarding; WebGUI separately controls Manager response for entry, reasoning, and completion groups, all default on without changing plugin registration | strict `[rabi:use <RoleId>]` controls, exact-session proactive binding, or an enabled plan completion binding | `src/context/rabiContextManager.ts`, `src/manager/codexHookContext.ts`, `plugins/rabi-codex-context/`, `docs/rabi-codex-context-plugin_en.md` |
| Plan-task completion reminder | experimental; code and mock path tested | the plan's independent business `taskBinding`, Codex Stop session/turn/cwd/final message, and exact persona Route/gateway binding | Stop Hook → Manager → role-panel timeline → Forwarding / AgentPacket → main persona assigns a plan secretary to consume the result and continue the original business task; routing deduplicates by session plus turn and never mutates the plan itself | plan POST/PATCH `taskBinding.completionHook` plus Route `codexHooks.planTaskCompletionEnabled`; secretaries are control-plane only, rotation/pause does not clear the business binding, and the loop requires no unmanaged actionable plan or idle actionable business task; real Desktop acceptance is still pending | `src/manager/codexHookContext.ts`, `src/manager/planTaskCompletionDelivery.ts`, `src/manager/controlPlaneRoutes.ts`, `plugins/rabi-codex-context/` |
| Copilot CLI | experimental | AgentPacket | local CLI process/output state | handler scan/config | `src/copilotCli.ts` |
| AstrBot | experimental | `ASTRBOT_URL`, Dashboard credentials, and required `ASTRBOT_SESSION_ID` | login followed only by ChatUI `POST /api/chat/send`; missing Session ID fails closed | handler scan/config | `src/agentAdapters/astrbotAdapter.ts`, `src/agentAdapters/managerApi.ts` |
| Marvis | stub/manual | AgentPacket | prompt file, clipboard, app focus | handler config | `src/marvis.ts` |
| Agent thread bridge and required-response requests | current; five-minute reminders still require local Hook end-to-end acceptance | `/api/agent/threads`, shared `messageSource`, and `data/.runtime/agent-requests.json` | non-empty `create` / `send` requests require a complete source. Agent sources require the Agent endpoint, session name, and complete session ID; Agent-to-Agent delivery also verifies `sourceThreadId`. Bodies start with `[消息源]`, then `[消息内容]`, and may create required-response requests | local Manager API, Codex `PreToolUse` / `Stop` | `src/shared/rabiMessage.ts`, `src/agentThreads.ts`, `src/agentRequests/`, `src/manager/agentCommunicationHookPolicy.ts`, `src/manager/controlPlaneRoutes.ts` |
| Message-processing board and delivery requirements | experimental; state machine, APIs, and WebGUI pass build tests | `data/.runtime/message-processing-board.json`, source/reply/attachment evidence, structured handoff, plan writes, and Outbox receipts | Message Agents, heartbeat, and WebGUI | explicit mentions, direct replies, private messages, and plan progress create required items; ordinary group discussion remains an Agent decision; NapCat images are materialized immediately and delivered as Desktop image input, with the focused discussion fragment ahead of broad history; incomplete source, reply-chain, or attachment review blocks reply, while unreadable media permits only retry or handoff; a group reply to an Agent-sent message adds the receipt's sender session as a pool-ranking preference; before a reply, Manager returns current bounded bidirectional context and binds short-lived approval to its version, complete session, destination, and payload. If the exact source is outside the recent window, only one same-Route record from the persona's formal group history may be restored; new messages, prior replies, missing/duplicate/conflicting records, or request changes fail closed. The board exposes worker, handoff, no-reply reason, send failure, overdue state, and idle-without-outcome. Canonical plan writes emit notifications without directory polling | `GET /api/message-processing/board`, `POST /api/message-processing/requirements/:id/outcome`, `GET/POST /api/message-processing/requirements/:id/send-context`, `/api/agent/threads`, `/api/agent/send`, `GET /api/agent/send/traces` | `src/napcatMedia.ts`, `src/messageProcessing/sourceEvidence.ts`, `src/messageProcessing/board.ts`, `src/messageProcessing/persistence.ts`, `src/messageProcessing/sendContextReview.ts`, `src/messageProcessing/sourceContextRecovery.ts`, `src/messageProcessing/managerClient.ts`, `src/messageAgentPool.ts`, Manager control plane, `ribiwebgui/src/components/MessageProcessingBoard.vue` |
| Plan API | current | handler/Manager call plus persona-private `plans/history/*.jsonl` and `plans/attachments/<planId>/` | five top-level states and managed attachments; Manager derives green `In progress`, blue `Awaiting package`, purple `Awaiting QA`, gray `Paused`, red `Awaiting approval`, and orange `Awaiting manual verification`, including counts, palettes, and ordering. External reasons remain internal `waitingFor` details. Content-changing plans enter blue package waiting only after applicable Main/Release/Art sync, SVN commit, and conflict-free readback; target-package inclusion proof moves them to purple QA. The QA receipt selects `send_qa_request` or `wait_for_qa_result` inside that stage. Other plan types keep their real workflows. WebGUI and Qt consume the DTO, use a neutral unknown state when `presentation` is absent, and never maintain a second classifier | `/api/roles/:roleId/plans`, `GET /api/roles/:roleId/plans/:planId/history`, `GET /api/roles/:roleId/plans/:planId/attachments/:attachmentId` | `src/roleKnowledge.ts`, `src/planAttachments.ts`, `src/roleKnowledgePresentation.ts`, `src/roleKnowledgePagination.ts`, `src/manager/planAttachmentRoutes.ts`, control plane |
| Plan feedback and QA verdicts | current | `data/roles/<RoleId>/plans/feedback/*.jsonl`, private `attachments/<feedbackId>/`, and managed attachments from the current plan | bound business task, persona Agent, WebGUI, and Qt tray | ordinary guidance and approval feedback is validated, persisted, and delivered through the original `taskBinding` without directly updating the plan. Manager consumes a verdict only when the current structured step is `qa-* / verify-*` and a user or external-source `approval_suggestion` states an explicit QA result. Failure or reproduction reopens investigation on the same plan, requests the minimum evidence, and continues the original task; only explicit QA pass completes it. Agent execution reports, guidance/responses, and bare `passed / verified` counters remain ordinary records | `GET/POST /api/roles/:roleId/plans/:planId/feedback` | `src/planFeedback.ts`, `src/manager/planQaFeedback.ts`, `src/manager/planApprovalFeedbackDelivery.ts`, Manager control plane, `src/routing/systemEventRules.ts` |
| Recent memory | current | handler/Manager call or recall hit | Markdown files with legacy JSON compatibility, `updatedAt`, `viewedAt`, `recalledAt` | `/api/roles/:roleId/memory/recent` | `src/roleKnowledge.ts` |
| Consolidated memory | current | consolidation result/read/recall | stable Markdown files with legacy JSON compatibility and recall timestamps | `/api/roles/:roleId/memory/consolidated` | `src/roleKnowledge.ts` |
| Memory consolidation | current automatic and explicit flow | earliest 72-hour Manager deadline, manual `memory-consolidation` trigger, or API request | freeze the 24-hour cohort at the original 72-hour trigger; automatically create and deliver the due run; result marks inputs and writes output; optional dedicated Codex memory task | `/api/roles/:roleId/memory/consolidation-*` | `src/roleKnowledge.ts`, `src/memoryConsolidationAgent.ts`, `src/manager/memoryConsolidationScheduler.ts`, Manager role API |
| Role skills | current | Markdown metadata and item reads | skill index/required-read context | `/api/roles/:roleId/skills` | `src/roleKnowledge.ts` |
| WebGUI locale and User Guide | current | browser `rabiroute:webgui:locale`, reviewed UI catalog, `docs/user-guide/*.md` | changes interface copy and `<html lang>` only | top-bar `中 / EN`, `/#/docs` | `ribiwebgui/src/i18n/*`, `LocaleSwitcher.vue`, `ProjectDocsPage.vue` |
| LAN WebGUI access | current, off by default | local `data/Config.json.webguiLan` switch and generated access key | after restart, changes Manager from loopback to LAN listening; a local loopback URL redirects to the preferred LAN IP while preserving the current Route/page; non-local control-plane requests require the key and rotation invalidates old links; changing Current Route preserves the page type and redirects `#/routes/<Route-config-name>/overview|adapters|persona|knowledge|speech|runtime` | Console **LAN WebGUI access**, copy Route Console/knowledge links, `GET/PATCH /api/webgui-access` | `src/manager/webguiLanAccess.ts`, `src/manager/controlPlaneRoutes.ts`, `ribiwebgui/src/managerApi.ts`, `ribiwebgui/src/webguiLanRedirect.ts`, `ribiwebgui/src/routeScopedNavigation.ts`, `ribiwebgui/src/App.vue`, `ribiwebgui/src/pages/OverviewPage.vue` |
| Route/packet preview | planned | future side-effect-free simulation | must not write logs, deliver, or refresh memory | future persona workbench | design document only |

## Boundary rules

- Current replacements are `GET/POST /api/plugins/reconciliation`, `/api/scan/agents`, `/api/scan/agents/dsh`, and `/api/fennenote/playback`. The old `/api/plugins/reconcile`, Agent availability endpoints, `/api/playback/request`, AstrBot plugin fallback, and deployment endpoint are removed.

- `adapterConfig.json` owns route runtime settings, the Agent list, `primaryAgentAdapter`, and local script permission; normalization guarantees that the Primary Agent remains in the configured list. `personaConfig.json` owns the optional avatar reference, persona automation rules, speech keywords, and recent-message limits.
- Matched ordinary endpoint messages are delivered immediately to the Primary Agent. When Codex is primary, delivery uses `steer` while a turn is active and `start` otherwise. A primary failure does not fall back to another Agent. Only explicit endpoint policy creates an exception, such as Heartbeat's busy-skip switch or speech keyword wake-up.
- `speechPushMode=hot` delivers every completed ASR segment. `keyword` still records every segment and delivers only when `speechTriggerKeywords` matches; an empty keyword list stays record-only.
- `recentMessageLimits` is persona-owned and independently configures ordinary logical endpoints. A zero value never disables recording; Heartbeat always omits history regardless of a legacy stored value.
- `agentRoleId` binds one route to one reusable role. A rule does not choose another role.
- RabiLink is a Manager-owned system transport. The glasses entry is an endpoint using that transport; RabiSpeech uses the same transport without entering message routing or an Agent.
- Codex adapter ID stays `codex`; Codex/ChatGPT Desktop is the required task owner.
- Desktop IPC is the only real-message Codex transport. App-server is limited to empty-task metadata bootstrap.
- A valid saved task ID plus workspace is stable; Desktop renames, stale index titles, and completed goals do not create duplicates.
- Outbox is the only normal external-reply path.
- `sent`, `draft`, `blocked`, and `failed` are results, not a persistent approval queue.
- Packet construction may refresh memory `viewedAt`; explicit memory consolidation can create a run. A future preview must avoid these side effects.
- Persistent plan-secretary control uses one writer per `planId` and allows different plans to proceed in parallel. Shared ledgers merge under a short lock with atomic replacement. Complete lock metadata is atomically published; stale or corrupt locks fail closed and require quiescent maintenance repair. Same-key claims and clarifications persist a reservation before delivery and never auto-resend an uncertain result. Audit compares before/after snapshots to distinguish stable invalid data from concurrent incomplete observations, so one active cycle never becomes a global barrier for audit or reconciliation.
- Tencent Sheet sources, direct or generic user requests, and work-group issues with a verified quoted claim all use the managed `register-external` action before secretary `begin → finish`. Work-group registration is restricted to the configured work group `<WORK_GROUP_ID>` and requires the real `sourceMessageId` plus a matching claim receipt with `status=sent` and `sentMessageId`; it also validates unique plan/task bindings, at least two deduplication passes, and the same workspace across input, plan, and business task before producing a `governanceVersion=3` mapping. Tencent stable-row keys and user-request canonical signatures keep their existing semantics, and `issue-threads.json` must not be edited manually.
- When a legacy claim used the unified Outbox but did not populate the dedicated receipt ledger, registration recovers it only if both the local outbound conversation record and a live NapCat message readback prove the same group, sent message, and quoted source. Input alone or one-sided logs cannot create a receipt, and recovery never resends the group message.
- Work-group `begin` identifies the business task through the registration mapping, plan binding, and the full Desktop task ID plus workspace. PangHu accepts `[PangHu][explicit task type] ...`; RabiRoute governance tasks currently accept only `[RabiRoute][Bug] ...`. New registrations reject an invalid live title. An invalid legacy title is migrated atomically under the issue-ledger lock only when the live title is valid for the same task and workspace. Titles do not replace stable identity, source, claim-receipt, deduplication, or uniqueness checks.
- WebGUI locale is only a browser UI preference. Route/persona IDs, rule names, templates, regexes, task names, paths, tokens, logs, and runtime values are not translated; User Guide selects the matching file under `docs/user-guide/`.

## Runtime data

Common route files:

```text
group-messages.jsonl
private-messages.jsonl
wecom-messages.jsonl
voice-transcripts.jsonl
heartbeat-events.jsonl
manual-trigger-events.jsonl
agent-packets.jsonl
outbox-adapter.log.jsonl
gateway-status.json
```

Role data:

```text
data/roles/<RoleId>/plans/
data/roles/<RoleId>/memory/
data/roles/<RoleId>/skills/
data/roles/<RoleId>/role-panel/
data/roles/<RoleId>/voice/voice-profile.json
data/roles/<RoleId>/voice/voice-identities.jsonl  # compatibility source for voice endpoint classification
data/roles/<RoleId>/voice/cache/tts-audio/
data/roles/<RoleId>/conversation/current.jsonl
data/roles/<RoleId>/conversation/archive/<firstSequence>~<lastSequence>.jsonl
data/roles/<RoleId>/conversation/archive/index.json
```

The conversation ledger is the automatic recent-context source. It is scoped by persona, logical endpoint, and conversation; inbound and outbound records count together. `current.jsonl` has no entry-count cap, while archives remain explicit-query evidence and are never read automatically. Persona TTS output under `voice/cache/tts-audio/` is a rebuildable 24-hour per-file cache; public read models expose only safe relative references and expected expiry, never host absolute paths. These are runtime/private sources of truth and are not public examples.

## Change-entry map

| Goal | Files to inspect first | Key constraint |
| --- | --- | --- |
| New message platform | `src/adapters/`, shared config model, endpoint scan/manager, Outbox if bidirectional | Normalize into common events; do not grow NapCat. |
| Change matching | `src/routing/routeDecision.ts` | Keep it deterministic and side-effect-free. |
| Change handler prompt | `src/routing/agentPacket.ts`, `src/roleKnowledge.ts` | Preserve thin user templates and reply context. |
| Change shared delivery wording | `src/shared/agentCommunicationPolicy.ts`, `src/agentThreads.ts`, `src/messageAgentPool.ts`, `src/shared/codexPlanAssistantSessions.ts` | State stable rules once. Keep API fields; move long explanations to docs or Skills. |
| Change outbound language-style validation | `src/languageStyleValidation.ts`, `src/agentSendLanguageStyle.ts`, `src/manager/languageStyleRoutes.ts` | `styleValidation=1` validates by default; `0` bypasses one request only. Failures stop before Outbox. |
| Change Codex delivery | `src/codexRuntime.ts`, `src/codexDesktopBridge.ts`; `src/codexAppServerClient.ts` only for empty-task metadata | Desktop IPC only for real messages; no shared port, per-route stdio, CLI, app-server execution fallback, or model override. |
| Change external send | `src/outbox.ts` and platform sender | Preserve policy, explicit targets, and audit statuses. |
| Change role knowledge | `src/roleKnowledge.ts`, Manager role API | Account for validation, timestamps, and consolidation side effects. |
| Change WebGUI config | Vue page/store plus shared schema | Update both language guides and examples when behavior changes. |

## New-feature checklist

1. Which layer owns the behavior?
2. What is the normalized input and source of truth?
3. What side effects occur?
4. Is maturity verified, experimental, stub, planned, or historical?
5. Which policy gates external actions?
6. Which logs and status fields make failure diagnosable?
7. Which bilingual public guides/examples must change?
8. Could preview or scan accidentally trigger live work or touch role memory?

## Search keywords

- persona / role / `agentRoleId`: persona binding, automation, plans, memories, and skills.
- automation / `automationRules` / schedule / `run_script`: persona automation, RouteDecision, Heartbeat, and script runtime.
- QQ / NapCat / OneBot: QQ ingress, route kinds, and Outbox replies.
- RabiLink / Relay / Rokid: Relay worker, observation input, and device output.
- WeCom / Weixin / Feishu: platform adapters and Outbox return paths.
- Codex / DSH / Copilot / AstrBot / Marvis: handler adapters.
- reply / outbound / approval: Outbox and Action Gate.
- memory / plan / skill / consolidation: Role Knowledge.
- replay / logs / delivery: evidence and runtime diagnostics.
